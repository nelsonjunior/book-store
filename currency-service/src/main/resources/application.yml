server:
  port: 8200
eureka:
  client:
    service-url:
      fetch-registry: true
      default-zone: http://localhost:8761/eureka
spring:
  application:
    name: currency-service
scheduler:
  cron: "0 0/1 * * * *"
